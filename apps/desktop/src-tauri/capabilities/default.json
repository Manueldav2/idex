{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set granted to the main IDEX window. We keep it tight: dialog + filesystem within $HOME, the shell open() helper for external links, OS info for platform detection. Everything else routes through our own typed commands so we control the surface area.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-read-dir",
    "fs:allow-exists",
    "fs:allow-mkdir",
    "shell:allow-open",
    "os:allow-platform",
    "os:allow-version"
  ]
}
